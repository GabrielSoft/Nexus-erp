# Development with Vite running directly on the host
VITE_API_URL="http://localhost:3333/api"

# When the frontend is served by the Docker/Nginx image, leave this variable unset:
# the application automatically uses the same-origin /api proxy.
